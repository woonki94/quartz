[334. Increasing Triplet Subsequence](https://leetcode.com/problems/increasing-triplet-subsequence/)
#array 
### Problem
Given an integer array `nums`, return `true` _if there exists a triple of indices_ `(i, j, k)` _such that_ `i < j < k` _and_ `nums[i] < nums[j] < nums[k]`. If no such indices exists, return `false`.

**Example 1:**

**Input:** nums = [1,2,3,4,5]
**Output:** true
**Explanation:** Any triplet where i < j < k is valid.

**Example 2:**

**Input:** nums = [5,4,3,2,1]
**Output:** false
**Explanation:** No triplet exists.

**Example 3:**

**Input:** nums = [2,1,5,0,4,6]
**Output:** true
**Explanation:** The triplet (3, 4, 5) is valid because nums[3] == 0 < nums[4] == 4 < nums[5] == 6.

**Constraints:**

- `1 <= nums.length <= 5 * 105`
- `-231 <= nums[i] <= 231 - 1`

**Follow up:** Could you implement a solution that runs in `O(n)` time complexity and `O(1)` space complexity?

### sketch

So its about finding length 3 subsequence that  is in order.

hmmm... how about 
1. first, make an sorted array `sorted_nums`
2. compare the position,,?

	[2,1,5,0,4,6]
	[0,1,2,4,5,6]

not quite valid idea......

the simplest idea would be brute force
1. at the current `i` search only the forward, and see if there's at least 2  increasing number than nums[i]
2. The problem could be....-> time complexity being O(n^2)

Lets implement it first and improve the idea.....

> Hard to make robust code.......
> for example : [1,5,0,4,1,3] -> when i = 2 : nums[i] = 0 , we check 4 and look for larger number 
> current : [0,4]
> .... after searching through all 1, 3, we need to consider the next case: [0,1]

This way the complexity might go up to O(n^3)


*Another approach*
- [1,5,0,4,1,3] 
- first = 1, second = 5 -> if meets smaller element , reset that as a first, and find second... and so on. Might work in O(n)....
- Lets try.

- Okay approach but -> need to check both first and second.
	- for example, let first = 0, second = 4
	- when we meet `1` , we don't set it to first, only change second value to current element.

	So starting at First = 0, second = 4


Current_element = 4, First = 0, Second = 4 
Current_element = 1, First = 0 ( no change in first since curr is larger than first!!!) , Second = 1
Current_element = 3, First = 0 (still no change), second = 1 ( also no change...). 

If passed all conditions -> there's a element larger than first and second -> Triplet exists


### Solution
```Python
class Solution(object):
    def increasingTriplet(self, nums):
        """
        :type nums: List[int]
        :rtype: bool
        """
        
        first = float('inf')
        second = float('inf')
        
        for i, num in enumerate(nums):
            if num <= first:
                first = num
            elif num <= second:
                second = num
            else:
                return True  
        
        return False
                
            
```

### Results

https://leetcode.com/problems/increasing-triplet-subsequence/submissions/1691756457

Not a best performance, but still O(n) , and acceptable range of speed..... not bad