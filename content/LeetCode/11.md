[11. Container With Most Water](https://leetcode.com/problems/container-with-most-water/)
#two-pointer

You are given an integer array `height` of length `n`. There are `n` vertical lines drawn such that the two endpoints of the `ith` line are `(i, 0)` and `(i, height[i])`.

Find two lines that together with the x-axis form a container, such that the container contains the most water.

Return _the maximum amount of water a container can store_.

**Notice** that you may not slant the container.

**Example 1:**

![](https://s3-lc-upload.s3.amazonaws.com/uploads/2018/07/17/question_11.jpg)

**Input:** height = [1,8,6,2,5,4,8,3,7]
**Output:** 49
**Explanation:** The above vertical lines are represented by array [1,8,6,2,5,4,8,3,7]. In this case, the max area of water (blue section) the container can contain is 49.

**Example 2:**

**Input:** height = [1,1]
**Output:** 1

**Constraints:**

- `n == height.length`
- `2 <= n <= 105`
- `0 <= height[i] <= 104`

### sketch

hmmmmm.  

so we are trying to get max containable water,
- x: end_idx - start_idx 
- y: min(height[start_idx], height[end_idx])
- container size: x * y

How we search for the max...

let start_idx = 0, end_idx = len-1

its better to move the idx that is smaller 
- if start_idx> end_idx : end_idx --
- else if start_idx < end_idx : start_idx ++


while start_idx == end_idx



### Solution
```python
class Solution(object):
    def maxArea(self, height):
        """
        :type height: List[int]
        :rtype: int
        """
        
        start_idx = 0
        end_idx = len(height)-1
        max = 0

        #print(min(height[start_idx],height[end_idx]))
        
        while start_idx != end_idx:
            x = end_idx - start_idx
            y = min(height[start_idx], height[end_idx])
            cont = x * y
            #print(x)
            #print(y)
            #print(cont)
            if cont > max:
                max = cont

            if y == height[start_idx]:
                start_idx +=1
            else:
                end_idx -=1
        

        return max
```

### Result
https://leetcode.com/problems/container-with-most-water/submissions/1705366811

Good both in terms of computation and memory


