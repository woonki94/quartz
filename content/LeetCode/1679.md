[1679. Max Number of K-Sum Pairs](https://leetcode.com/problems/max-number-of-k-sum-pairs/)
#two-pointer 


### Problem
You are given an integer array `nums` and an integer `k`.

In one operation, you can pick two numbers from the array whose sum equals `k` and remove them from the array.

Return _the maximum number of operations you can perform on the array_.

**Example 1:**

**Input:** nums = [1,2,3,4], k = 5
**Output:** 2
**Explanation:** Starting with nums = [1,2,3,4]:
- Remove numbers 1 and 4, then nums = [2,3]
- Remove numbers 2 and 3, then nums = []
There are no more pairs that sum up to 5, hence a total of 2 operations.

**Example 2:**

**Input:** nums = [3,1,3,4,3], k = 6
**Output:** 1
**Explanation:** Starting with nums = [3,1,3,4,3]:
- Remove the first two 3's, then nums = [1,4,3]
There are no more pairs that sum up to 6, hence a total of 1 operation.

**Constraints:**

- `1 <= nums.length <= 105`
- `1 <= nums[i] <= 109`
- `1 <= k <= 109`


### Sketch

first need to see how to remove the element
> Use .pop(idx) to pop element

init to :
start_idx = 0
end_idx = len(nums)-1

and search for the pair that sums up to k
if found pair
pop(start_idx)
pop(end_idx)

start_idx = 0
end_idx = len(nums)-1

if not
start_idx ++
end_idx --

repeat until start_idx == end_idx

[1,2,3,4]

start_idx = 0
end_idx = 3

1 + 4 == 5 :
pop start -> [2,3,4]
pop (end-1) -> [3,4]

start = 0
end = len(num) -1 = 1


*not working*

it does not search all the combinations..... not robust

need something that searches for every combination possible:
- It would have worse complexity but, no other choice

so let 
start_idx = 0 and mov_idx = start_idx +1

while start_idx != len(nums)-2

if found pair: 
pop(start_idx)
pop(end_idx)

start_idx = 0 
mov_idx = start_idx +1

if not:
mov_idx +=1

if mov_idx == len(nums)-1
start_idx +=1
mov_idx = start_idx +1


It will search all combinations


*Failed* -> due to time limit

Need another way........

using Hashmap!!

first traverse through the list, and store element and occurrence

i.e.) [1,2,3,4]
Key value
1  1
2  1
3 1
4 1

i.e. 2) [1,1,1,1]
Key value
1 4


Then with this occurrence map

find key pairs that the sum is equal to k


### Solution

```python
class Solution(object):
    def maxOperations(self, nums, k):
        """
        :type nums: List[int]
        :type k: int
        :rtype: int
        """
        map = dict()
        count = 0
        for num in nums:
            if num in map:
                map[num] +=1
            else:
                map[num] = 1

        for num in nums:
            #print(map)
            if (k-num) in map:
                if num == (k-num):
                    if map[num] >1:
                        count+=1
                        map[num] -=2
                else:
                    if map[num] > 0 and map[k-num] > 0:
                        count +=1
                        map[num] -=1
                        map[k-num] -=1
                        #print(map)
        return count
```

### Results
https://leetcode.com/problems/max-number-of-k-sum-pairs/submissions/1705517824

Fast

O(n)