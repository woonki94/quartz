[238. Product of Array Except Self](https://leetcode.com/problems/product-of-array-except-self/)
#array
### Problem
Given an integer array `nums`, return _an array_ `answer` _such that_ `answer[i]` _is equal to the product of all the elements of_ `nums` _except_`nums[i]`.

The product of any prefix or suffix of `nums` is **guaranteed** to fit in a **32-bit** integer.

You must write an algorithm that runs in `O(n)` time and without using the division operation.

**Example 1:**

**Input:** nums = [1,2,3,4]
**Output:** [24,12,8,6]

**Example 2:**

**Input:** nums = [-1,1,0,-3,3]
**Output:** [0,0,9,0,0]

**Constraints:**

- `2 <= nums.length <= 105`
- `-30 <= nums[i] <= 30`
- The input is generated such that `answer[i]` is **guaranteed** to fit in a **32-bit** integer.

**Follow up:** Can you solve the problem in `O(1)` extra space complexity? (The output array **does not** count as extra space for space complexity analysis.)

### sketch

1. Multiply all other elements except i'th 
2. need to run in O(n)

first get prod of all elements-> divide by myself

but the problem is when there's a 0...... <- need exception
	also need handling 
		1. when there's only 1 zero
		2. there's more than 2 zeros


### Solution
```python
import numpy as np

class Solution(object):
    def productExceptSelf(self, nums):
        """
        :type nums: List[int]
        :rtype: List[int]
        """
        prod_all = 1
        zero_idx = []
        ret = []
        for i, num in enumerate(nums):
            if num == 0:
                zero_idx.append(i)
            else:
                prod_all *= num

        
        if not zero_idx:
            ret = [prod_all/num for num in nums]
        
        else:
            ret = [0] * len(nums)
            if len(zero_idx) >= 2:
                return ret
            else:
                for i in range(len(zero_idx)):
                    idx = zero_idx[i]
                    ret[idx] = prod_all

        return ret
        
            
```

### Result

https://leetcode.com/problems/product-of-array-except-self/submissions/1691415364


Not bad.. 

easy